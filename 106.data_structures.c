#include <stdio.h>

int main(){
  // Data structures
  //data => quantities, characters or symbols
  // information => data with meaningful information from it => here use data structures

  //data structure => systematic way to organize data so that it can be used efficiently. We can use arrays, because we can manage data in appropiate way, actually in a sequential way. Instead of creating multiple variables of same type. Why not create an array to store all the values.

  //We use to store an image as a bitmap, undo - redo 
  //bitmaps images are stored as a series of tiny dots called pixels.
  //frienship in social media

  

  return 0;
}