#include "stdio.h"

extern int sp;
extern double val[];
const int BUFSIZE = 8;

static char buf[BUFSIZE]; // Not visible for other files. Encapsulation
static int bufo = 0;

int main(){

  return 0;
}